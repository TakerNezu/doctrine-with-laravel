services:
  integrating-doctrine-with-laravel:
    container_name: integrating-doctrine-with-laravel
    build: ./
    ports:
      - '80:80'
      - '5173:5173'
    volumes:
      - ./:/var/www/html
    working_dir: /var/www/html
